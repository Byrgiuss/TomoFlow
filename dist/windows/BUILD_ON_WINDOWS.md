# Windows Build Note

This repository includes a ready Mac build in `dist/mac/TomoFlow.app`.

Windows executable (`dist\\TomoFlow\\TomoFlow.exe`) must be built on a Windows machine.
Use:

```bat
build_windows_exe.bat
```

The script automatically generates `assets/TomoFlowLogo.ico` if missing.
